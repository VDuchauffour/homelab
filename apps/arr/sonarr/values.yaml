---
env:
  TZ: Europe/Paris

persistence:
  config:
    enabled: true
    type: pvc
    mountPath: /config
    readOnly: false
    storageClass: zfs-vm-pool-sonarr-config
  media:
    enabled: true
    type: pvc
    readOnly: false
    storageClass: zfs-tank-sonarr
    accessMode: ReadWriteOnce
    size: 25Gi

podSecurityContext:
  runAsUser: 1000
  runAsGroup: 1000
  fsGroup: 1000

ingress:
  main:
    enabled: true
    primary: true
    annotations:
      kubernetes.io/ingress.class: traefik
      cert-manager.io/cluster-issuer: mkcert-ca
    labels: {}
    ingressClassName: traefik
    hosts:
      - host: sonarr.home.arpa
        paths:
          - path: /
            pathType: Prefix
    tls:
      - hosts:
          - sonarr.home.arpa
        secretName: sonarr-mkcert-tls
