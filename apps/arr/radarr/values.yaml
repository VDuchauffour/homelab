---
env:
  TZ: Europe/Paris

persistence:
  config:
    enabled: true
    type: pvc
    mountPath: /config
    readOnly: false
    storageClass: zfs-vm-pool-radarr-config
  media:
    enabled: true
    type: pvc
    readOnly: false
    storageClass: zfs-tank-radarr
    accessMode: ReadWriteOnce
    size: 25Gi

podSecurityContext:
  runAsUser: 1000
  runAsGroup: 1000
  fsGroup: 1000

ingress:
  main:
    enabled: true
    primary: true
    annotations:
      kubernetes.io/ingress.class: traefik
      cert-manager.io/cluster-issuer: mkcert-ca
    labels: {}
    ingressClassName: traefik
    hosts:
      - host: radarr.home.arpa
        paths:
          - path: /
            pathType: Prefix
    tls:
      - hosts:
          - radarr.home.arpa
        secretName: radarr-mkcert-tls
