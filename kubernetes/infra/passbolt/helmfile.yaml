---
repositories:
  - name: passbolt
    url: https://download.passbolt.com/charts/passbolt

releases:
  - name: passbolt
    namespace: passbolt
    createNamespace: true
    chart: passbolt/passbolt
    version: 2.1.0
    values:
      - values.yaml
