---
repositories:
  - name: jetstack
    url: https://charts.jetstack.io
releases:
  - name: cert-manager
    namespace: cert-manager
    chart: jetstack/cert-manager
    version: v1.20.2
    createNamespace: true
    values:
      - installCRDs: true
        prometheus:
          enabled: true
          servicemonitor:
            enabled: true
