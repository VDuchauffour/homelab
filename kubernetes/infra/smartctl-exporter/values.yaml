---
serviceMonitor:
  enabled: true
  interval: 60s
  scrapeTimeout: 30s

prometheusRules:
  enabled: true

nodeSelector:
  kubernetes.io/hostname: ref+envsubst://$SINGLE_NODE_NAME

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi
