apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wakatime-exporter.fullname" . }}-dashboard
  labels:
    {{- include "wakatime-exporter.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  wakatime.json: |-
    {{- .Files.Get "dashboards/wakatime.json" | nindent 4 }}
