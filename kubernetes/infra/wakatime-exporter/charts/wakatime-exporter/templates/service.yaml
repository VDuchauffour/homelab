apiVersion: v1
kind: Service
metadata:
  name: {{ include "wakatime-exporter.fullname" . }}
  labels:
    {{- include "wakatime-exporter.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "wakatime-exporter.selectorLabels" . | nindent 4 }}
