---
repositories:
  - name: mojo2600
    url: https://mojo2600.github.io/pihole-kubernetes/

releases:
  - name: pihole
    namespace: pihole
    createNamespace: true
    chart: mojo2600/pihole
    version: 2.35.0
    values:
      - values.yaml
