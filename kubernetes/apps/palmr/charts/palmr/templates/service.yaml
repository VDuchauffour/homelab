apiVersion: v1
kind: Service
metadata:
  name: {{ include "palmr.fullname" . }}
  labels:
    {{- include "palmr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "palmr.selectorLabels" . | nindent 4 }}
