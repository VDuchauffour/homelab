apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "openchamber.fullname" . }}-workdir
  labels:
    {{- include "openchamber.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.workdir.accessMode | quote }}
  storageClassName: {{ .Values.persistence.workdir.storageClass | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.workdir.size | quote }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "openchamber.fullname" . }}-worktree
  labels:
    {{- include "openchamber.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.worktree.accessMode | quote }}
  storageClassName: {{ .Values.persistence.worktree.storageClass | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.worktree.size | quote }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "openchamber.fullname" . }}-config
  labels:
    {{- include "openchamber.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.config.accessMode | quote }}
  storageClassName: {{ .Values.persistence.config.storageClass | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.config.size | quote }}
