apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "openchamber.fullname" . }}-config
  labels:
    {{- include "openchamber.labels" . | nindent 4 }}
data:
  opencode.json: |
    {{- index .Values "config-json" | nindent 4 }}
