---
image:
  repository: nextcloud
  flavor: apache
  pullPolicy: IfNotPresent

nextcloud:
  # Use supplementalGroups instead of fsGroup to avoid recursive ownership
  # changes on the large NFS media volume (fsGroup=33 would trigger a full
  # walk since the NFS root is owned by 1000:1000, not 33)
  podSecurityContext:
    runAsUser: 33
    runAsGroup: 33
    supplementalGroups:
      - 33
      - 1000

  host: nextcloud.ref+envsubst://$LOCAL_DOMAIN_NAME
  username: admin
  password: ref+scw:///homelab/nextcloud#/admin-password

  trustedDomains:
    - nextcloud.ref+envsubst://$LOCAL_DOMAIN_NAME
    - nextcloud.ref+envsubst://$PUBLIC_DOMAIN_NAME

  defaultConfigs:
    redis.config.php: false

  extraVolumes:
    - name: media-library
      persistentVolumeClaim:
        claimName: media-library

  extraVolumeMounts:
    - name: media-library
      mountPath: /media

  configs:
    proxy.config.php: |-
      <?php
      $CONFIG = array (
        'overwriteprotocol' => 'https',
        'trusted_proxies' => ['10.0.0.0/8'],
        'default_phone_region' => 'FR',
      );
    redis.config.php: |-
      <?php
      $CONFIG = array (
        'memcache.local' => '\\OC\\Memcache\\APCu',
        'memcache.distributed' => '\\OC\\Memcache\\Redis',
        'memcache.locking' => '\\OC\\Memcache\\Redis',
        'redis' => array (
          'host' => 'nextcloud-redis-master.nextcloud.svc.cluster.local',
          'port' => 6379,
        ),
      );

internalDatabase:
  enabled: false

externalDatabase:
  enabled: true
  type: postgresql
  host: cnpg-cluster0-rw.cnpg-clusters.svc.cluster.local
  port: 5432
  user: nextcloud
  password: ref+scw:///homelab/cnpg#/nextcloud-password
  database: nextcloud

redis:
  enabled: false

persistence:
  enabled: true
  storageClass: zfs-configs-clusters-k3s-dynamic
  accessMode: ReadWriteOnce
  size: 20Gi

ingress:
  enabled: true
  className: traefik
  annotations:
    kubernetes.io/ingress.class: traefik
    cert-manager.io/cluster-issuer: mkcert-ca
  tls:
    - secretName: nextcloud-mkcert-tls
      hosts:
        - nextcloud.ref+envsubst://$LOCAL_DOMAIN_NAME
  hosts:
    - host: nextcloud.ref+envsubst://$LOCAL_DOMAIN_NAME
      paths:
        - path: /
          pathType: Prefix

cronjob:
  enabled: true

phpClientHttpsFix:
  enabled: true
  protocol: https

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    memory: 1Gi

livenessProbe:
  enabled: true
  initialDelaySeconds: 120
  periodSeconds: 30
  timeoutSeconds: 10
readinessProbe:
  enabled: true
  initialDelaySeconds: 60
  periodSeconds: 15
  timeoutSeconds: 10
