apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "papra.fullname" . }}-config
  labels:
    {{- include "papra.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
