apiVersion: v1
kind: Secret
metadata:
  name: {{ include "papra.fullname" . }}-secret
  labels:
    {{- include "papra.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
