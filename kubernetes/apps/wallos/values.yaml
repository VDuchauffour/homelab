---
config:
  TZ: Europe/Paris

persistence:
  enabled: true
  storageClass: zfs-vm-pool-dynamic
  size: 1Gi

resources:
  requests:
    cpu: 10m
    memory: 64Mi
  limits:
    memory: 256Mi

image:
  repository: bellamy/wallos
  pullPolicy: IfNotPresent
  tag: latest

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: traefik
  annotations:
    kubernetes.io/ingress.class: traefik
    cert-manager.io/cluster-issuer: mkcert-ca
  hosts:
    - host: wallos.ref+envsubst://$LOCAL_DOMAIN_NAME
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - wallos.ref+envsubst://$LOCAL_DOMAIN_NAME
      secretName: wallos-mkcert-tls
