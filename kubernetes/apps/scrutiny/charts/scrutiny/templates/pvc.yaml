{{- if .Values.persistence.config.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "scrutiny.fullname" . }}-config
  labels:
    {{- include "scrutiny.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.persistence.config.storageClass }}
  storageClassName: {{ .Values.persistence.config.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.config.size }}
{{- end }}
---
{{- if .Values.persistence.influxdb.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "scrutiny.fullname" . }}-influxdb
  labels:
    {{- include "scrutiny.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.persistence.influxdb.storageClass }}
  storageClassName: {{ .Values.persistence.influxdb.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.influxdb.size }}
{{- end }}
