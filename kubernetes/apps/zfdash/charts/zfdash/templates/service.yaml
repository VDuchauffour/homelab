apiVersion: v1
kind: Service
metadata:
  name: {{ include "zfdash.fullname" . }}
  labels:
    {{- include "zfdash.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zfdash.selectorLabels" . | nindent 4 }}
