apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tracearr.fullname" . }}-config
  labels:
    {{- include "tracearr.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
