apiVersion: v1
kind: Service
metadata:
  name: {{ include "peanut.fullname" . }}
  labels:
    {{- include "peanut.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "peanut.selectorLabels" . | nindent 4 }}
