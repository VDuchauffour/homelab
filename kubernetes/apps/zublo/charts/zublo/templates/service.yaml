apiVersion: v1
kind: Service
metadata:
  name: {{ include "zublo.fullname" . }}
  labels:
    {{- include "zublo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zublo.selectorLabels" . | nindent 4 }}
