---
ingress:
  enabled: true
  className: traefik
  url: bentopdf.ref+envsubst://$LOCAL_DOMAIN_NAME
  annotations:
    kubernetes.io/ingress.class: traefik
    cert-manager.io/cluster-issuer: mkcert-ca
  tls:
    enabled: true
    secretName: bentopdf-mkcert-tls
