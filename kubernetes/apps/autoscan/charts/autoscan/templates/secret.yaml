{{- if .Values.autoscan }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "autoscan.fullname" . }}-config-json
  labels:
    {{- include "autoscan.labels" . | nindent 4 }}
type: Opaque
stringData:
  config.json: |
{{ .Values.autoscan | toPrettyJson | indent 4 }}
{{- end }}
