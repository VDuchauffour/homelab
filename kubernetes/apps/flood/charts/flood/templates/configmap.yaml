apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "flood.fullname" . }}-config
  labels:
    {{- include "flood.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
