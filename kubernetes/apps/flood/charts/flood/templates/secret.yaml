apiVersion: v1
kind: Secret
metadata:
  name: {{ include "flood.fullname" . }}-secret
  labels:
    {{- include "flood.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
