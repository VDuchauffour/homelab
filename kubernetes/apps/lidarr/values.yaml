---
env:
  TZ: Europe/Paris

image:
  repository: linuxserver/lidarr
  tag: latest
  pullPolicy: IfNotPresent

persistence:
  config:
    enabled: true
    type: pvc
    mountPath: /config
    readOnly: false
    storageClass: zfs-vm-pool-dynamic
    size: 1Gi
  media:
    enabled: true
    type: pvc
    mountPath: /media
    readOnly: false
    existingClaim: media-library

podSecurityContext:
  runAsUser: 1000
  runAsGroup: 1000
  fsGroup: 1000

ingress:
  main:
    enabled: true
    primary: true
    annotations:
      kubernetes.io/ingress.class: traefik
      cert-manager.io/cluster-issuer: mkcert-ca
    labels: {}
    ingressClassName: traefik
    hosts:
      - host: lidarr.ref+envsubst://$LOCAL_DOMAIN_NAME
        paths:
          - path: /
            pathType: Prefix
    tls:
      - hosts:
          - lidarr.ref+envsubst://$LOCAL_DOMAIN_NAME
        secretName: lidarr-mkcert-tls
