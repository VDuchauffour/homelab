apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "opencode.fullname" . }}
  labels:
    {{- include "opencode.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "opencode.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "opencode.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: install-packages
          image: "{{ .Values.openchamber.initImage.repository }}:{{ .Values.openchamber.initImage.tag }}"
          imagePullPolicy: {{ .Values.openchamber.initImage.pullPolicy }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              export DEBIAN_FRONTEND=noninteractive
              apt-get update && apt-get install -y --no-install-recommends python3 make g++ nodejs npm
              npm install -g node-gyp
              bun add -g {{ .Values.openchamber.package }} opencode-ai@latest
              rm -rf /var/lib/apt/lists/*
              {{- range .Values.postInstall }}
              {{ . }}
              {{- end }}
              mkdir -p /bun-home/.local/share/opencode/bin
              mkdir -p /bun-home/.ssh && chmod 700 /bun-home/.ssh
          env:
            - name: HOME
              value: /bun-home
            - name: BUN_INSTALL_BIN
              value: /bun-home/bin
          volumeMounts:
            - name: bun-global
              mountPath: /bun-home
            - name: config
              mountPath: /bun-home/.config/opencode
              subPath: opencode
            - name: config
              mountPath: /bun-home/.config/openchamber
              subPath: openchamber
            - name: config
              mountPath: /bun-home/.local/share/opencode
              subPath: data
            - name: config
              mountPath: /bun-home/.ssh
              subPath: ssh
      containers:
        - name: openchamber
          image: "{{ .Values.openchamber.image.repository }}:{{ .Values.openchamber.image.tag }}"
          imagePullPolicy: {{ .Values.openchamber.image.pullPolicy }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              export DEBIAN_FRONTEND=noninteractive
              apt-get update && apt-get install -y --no-install-recommends git neovim openssh-client gh git-delta && rm -rf /var/lib/apt/lists/*
              export PATH="/bun-home/bin:$PATH"
              exec openchamber --port {{ .Values.openchamber.port }}
          env:
            - name: HOME
              value: /bun-home
            - name: SHELL
              value: /bin/bash
            - name: GIT_CONFIG_COUNT
              value: "1"
            - name: GIT_CONFIG_KEY_0
              value: safe.directory
            - name: GIT_CONFIG_VALUE_0
              value: "*"
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          workingDir: /projects
          ports:
            - name: http
              containerPort: {{ .Values.openchamber.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
          volumeMounts:
            - name: bun-global
              mountPath: /bun-home
            - name: config
              mountPath: /bun-home/.config/opencode
              subPath: opencode
            - name: config
              mountPath: /bun-home/.config/openchamber
              subPath: openchamber
            - name: workdir
              mountPath: /projects
            - name: config
              mountPath: /bun-home/.local/share/opencode
              subPath: data
            - name: config
              mountPath: /bun-home/.ssh
              subPath: ssh
            - name: worktree
              mountPath: /bun-home/.local/share/opencode/worktree
            {{- if .Values.gitConfig.hostPath }}
            - name: gitconfig
              mountPath: /bun-home/.gitconfig
              readOnly: true
            {{- end }}
          {{- with .Values.resources.openchamber }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: bun-global
          emptyDir: {}
        - name: config
          persistentVolumeClaim:
            claimName: {{ include "opencode.fullname" . }}-config
        - name: workdir
          persistentVolumeClaim:
            claimName: {{ include "opencode.fullname" . }}-workdir
        - name: worktree
          persistentVolumeClaim:
            claimName: {{ include "opencode.fullname" . }}-worktree
        {{- if .Values.gitConfig.hostPath }}
        - name: gitconfig
          hostPath:
            path: {{ .Values.gitConfig.hostPath }}
            type: ""
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
